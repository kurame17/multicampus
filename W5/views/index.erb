<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
        <title>Blog</title>
    </head>
    <body>
        <h2>블로그에 오신 것을 환영합니다.</h2>
        <a href="/new">글쓰러 가기</a>
        <hr />
        <% @posts.each do |post|%>
            <p><%= post.id %></p>
            <p><%= post.title %></p>
            <p><%= post.content %></p>
            <p><%= post.created_at %></p>
            <a href="/posts/<%=post.id%>">글 보러가기 </a>
            <a href="/destroy/<%=post.id%>"> [X] </a>
            <a href="/edit/<%=post.id %>"> 수정하러 가기</a>
        <hr />
        <% end %>
    </body>
</html>
